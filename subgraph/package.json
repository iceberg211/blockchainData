{
  "name": "subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020/ heweif/blockchain-data-storage",
    "remove-local": "graph remove --node http://localhost:8020/ heweif/blockchain-data-storage",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 heweif/blockchain-data-storage"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  }
}